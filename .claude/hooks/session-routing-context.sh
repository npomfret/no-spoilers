#!/usr/bin/env bash
set -euo pipefail

cat <<'EOF'
{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"Routing baseline: prefer .claude/skills, .claude/agents, and .claude/rules over ad-hoc execution. For substantial work, load pattern-governance-reference first and identify the approved local pattern before editing. Reuse or refactor shared boundaries instead of adding a second implementation style. Use codebase-explorer for pattern lookup, implement-apple-change for Apple-platform implementation/refactor work, build-verify for compile/build confidence, test-changes for targeted behavior-risk verification, and review-working-tree or pattern-compliance-reviewer for read-only drift checks. Report exact command evidence before claiming completion."}}
EOF
